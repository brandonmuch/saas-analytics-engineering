name: 'saas_analytics_engineering'
version: '1.0.0'
config-version: 2

profile: 'saas-analytics-engineering'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  saas_analytics_engineering:
    staging:
      +materialized: view
    marts:
      +materialized: table